# Python-Abhängigkeiten für die Vercel Serverless Function (frontend/api/index.py).
# Vercels Python-Builder braucht diese Datei im Projekt-Root (= "Root Directory",
# hier `frontend/`). Inhalt identisch zu ../requirements-api.txt (siehe dort für
# die ausführliche Begründung) — Vercel liest keine Dateien außerhalb des
# Projekt-Roots, daher hier dupliziert statt referenziert.

fastapi>=0.137
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
pydantic>=2.11
asyncpg>=0.29
httpx>=0.28
