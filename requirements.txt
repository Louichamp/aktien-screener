# Louichamp Screener — Laufzeit- und Test-Abhängigkeiten
# Produktion: PostgreSQL via asyncpg; Tests: SQLite via aiosqlite.

# --- API / Persistenz ---
fastapi>=0.137
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
pydantic>=2.11
asyncpg>=0.29          # PostgreSQL-Treiber (Produktion)

# --- Migrationen / Konfiguration ---
alembic>=1.13
python-dotenv>=1.0

# --- Markt-Daten-Provider ---
# Yahoo Finance (Default, KOSTENLOS, kein Key) für das volle Universum:
yfinance>=0.2          # OHLCV + Fundamentaldaten + News
pandas>=2.0            # von yfinance/Kronos genutzt
lxml>=5.0              # HTML/CSV-Parsing der Universe-Quellen
# Financial Modeling Prep (optional, DATA_PROVIDER=fmp) nutzt httpx (s.u.).

# --- Kronos-Inferenz (PyTorch) — OPTIONAL, NICHT für Standardbetrieb nötig ---
# compute_scores.py nutzt fest den torch-freien StatisticalForecaster (GBM aus
# echten Log-Renditen) — kein GPU/Torch-Bedarf im Batch-Job (GitHub-Actions-
# Free-Runner hat keine GPU, torch allein ist >2GB). Nur wer selbst-gehostet mit
# eigener GPU das echte Kronos-Modell nutzen will (FORECAST_BACKEND=kronos),
# braucht das hier — lazy importiert, daher ohne Installation kein Fehler:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install transformers numpy
# torch>=2.2
# transformers>=4.40
# numpy>=1.26

# --- Tests ---
httpx>=0.28
aiosqlite>=0.20        # SQLite-Async-Treiber (Tests, JSONB-Fallback)
pytest>=8.0
pytest-asyncio>=0.24
